server:
  port: 8761 
eureka:
  instance:
      hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl: #al utilizar esta configuracion, cambias el puerto en los demas clientes con el SERVER_PORT:0
      defaultZone:  http://${eureka.instance.hostname}:${server.port}/eureka/ 

                    #Para usar esta configuracion en el server, en los clientes defines el port que quieras, 8001, 8002, 8003
                    # http://localhost:8761/eureka/ 
                                                              
    
    
    

